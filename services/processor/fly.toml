# FluxTape stream processor (Go) — Fly.io worker
app = "fluxtape-processor"
primary_region = "iad"

[build]
  dockerfile = "../Dockerfile.go"
  [build.args]
    SVC = "processor"

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory = "256mb"
